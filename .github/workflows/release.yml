name: Build and Release

on:
  push:
    tags:
      - 'v*'

# 设置环境变量
env:
  CARGO_INCREMENTAL: 0
  RUST_BACKTRACE: short

jobs:
  # 构建 macOS (Apple Silicon M芯片)
  build-macos:
    runs-on: macos-14  # macos-14 默认使用 M1 芯片
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Install frontend dependencies
        run: npm ci

      - name: Build Tauri App for macOS (ARM64)
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'RoxyBrowser Manager ${{ github.ref_name }}'
          releaseBody: |
            ## 更新日志
            
            请查看 commit 历史了解详细更改。
            
            ## 下载说明
            
            - **macOS (Apple Silicon/M芯片)**: 下载 `.dmg` 文件
            - **Windows**: 下载 `.msi` 或 `.exe` 安装程序
          releaseDraft: false
          prerelease: false
          args: --target aarch64-apple-darwin

      - name: Upload macOS artifacts
        uses: actions/upload-artifact@v4
        with:
          name: macos-arm64-artifacts
          path: |
            src-tauri/target/aarch64-apple-darwin/release/bundle/dmg/*.dmg
            src-tauri/target/aarch64-apple-darwin/release/bundle/macos/*.app
          if-no-files-found: error

  # 构建 Windows x64
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Install frontend dependencies
        run: npm ci

      - name: Build Tauri App for Windows
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'RoxyBrowser Manager ${{ github.ref_name }}'
          releaseBody: |
            ## 更新日志
            
            请查看 commit 历史了解详细更改。
            
            ## 下载说明
            
            - **macOS (Apple Silicon/M芯片)**: 下载 `.dmg` 文件
            - **Windows**: 下载 `.msi` 或 `.exe` 安装程序
          releaseDraft: false
          prerelease: false

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-x64-artifacts
          path: |
            src-tauri/target/release/bundle/msi/*.msi
            src-tauri/target/release/bundle/nsis/*.exe
          if-no-files-found: error

  # 创建统一的 Release（可选：如果需要将所有产物放在一个 Release 中）
  create-release:
    needs: [build-macos, build-windows]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Display structure of downloaded files
        run: ls -R artifacts

      - name: Create or Update Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          name: RoxyBrowser Manager ${{ github.ref_name }}
          body: |
            ## 🚀 RoxyBrowser Manager ${{ github.ref_name }}
            
            ### 📥 下载说明
            
            | 平台 | 文件 | 说明 |
            |------|------|------|
            | **macOS (Apple Silicon)** | `.dmg` | 适用于 M1/M2/M3 芯片的 Mac |
            | **Windows** | `.msi` | Windows 安装程序 (推荐) |
            | **Windows** | `.exe` | Windows NSIS 安装程序 |
            
            ### 📝 更新日志
            
            请查看 [Commits](https://github.com/${{ github.repository }}/commits/${{ github.ref_name }}) 了解详细更改。
            
            ### ⚠️ 安装说明
            
            **macOS 用户**: 如果遇到"无法打开"的提示，请在系统偏好设置 > 安全性与隐私 中允许运行。
            
            **Windows 用户**: 如果遇到 SmartScreen 警告，请选择"仍要运行"。
          draft: false
          prerelease: false
          files: |
            artifacts/**/*.dmg
            artifacts/**/*.msi
            artifacts/**/*.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
